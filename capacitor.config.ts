import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hukumnamaai.studio',
  appName: 'Hukumnama AI Studio',
  webDir: 'dist'
};

export default config;
